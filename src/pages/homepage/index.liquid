<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>slow stitch club</title>
    <style>
      * {
        font-family: sans-serif;
      }
    </style>
  </head>
  <body>
    <img src="/img/logomark.png" />
    <h1>slow stitch club</h1>

    <h2>
      {{ title }}
    </h2>

    <p>
      {{ intro }}
    </p>

    <main>
      <ul>
        {% for item in links %}
        <li>
          <a href="{{ item.link }}"
            >{{ item.name }}
            {% if item.photo %}
            <img src="{{ item.photo | replace: 'src/','' }}"/>
            {% endif %}
          </a>
        </li>
        {% endfor %}
      </ul>
    </main>

    <footer>
      <p>
        {{ footer.title }}
      </p>

      <div class="newsletter__signup">
        <p>
          {{newsletter.intro}}
        </p>
        <form class="newsletter-signup svelte-15xaass" method="post">
          <input
            type="hidden"
            name="form-name"
            value="form 1"
            class="svelte-15xaass"
          />
          <input
            class="form-input svelte-15xaass"
            name="email"
            type="email"
            placeholder="Enter your email address"
            required=""
          />
          <svg
            class="line svelte-15xaass"
            width="300"
            height="47"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M2 21.924c12.106.74 22.98.285 33.986-.002 42.205-1.1 84.905-2.214 129.852-1.213 43.743.975 88.815 2.612 132.514 4.813"
              stroke="#000"
              stroke-linecap="round"
              stroke-dasharray="10 14"
              class="svelte-15xaass"
            ></path>
          </svg>
          <button type="submit" class="svelte-15xaass">
            <svg
              width="25"
              height="21"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
              class="svelte-15xaass"
            >
              <path
                d="M1.074 8.36c7.617.425 15.596-.13 23.02.285M14.5 1.098c2.306 2.118 4.41 4.33 7.034 6.027 2.103 1.36 2.003 1.307.197 3.1-2.735 2.713-5.351 5.339-6.758 8.927"
                stroke="#000"
                stroke-width="1.4"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="svelte-15xaass"
              ></path>
            </svg>
          </button>
        </form>
      </div>

      <ul>
        {% for item in footer.links %}
        <li>
          <a href="{{ item.link }}" target="_blank">{{ item.name }}</a>
        </li>
        {% endfor %}
      </ul>

      <div>&copy; slow stitch club {{ "now" | date: "%Y" }}</div>
    </footer>
  </body>
</html>
